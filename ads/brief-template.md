# Ad concept — <CONCEPT NAME>

> Copy, fill every `<...>`, delete this line. The **Claims** table must pass the
> honesty rule in `ads/README.md`.

- **Phase:** `<GTM.md phase>`
- **Objective / goal:** `<the /v1/analytics goal this ad drives, e.g. signup>`
- **Audience:** `<cold / warm / hot; which segment>`
- **Channel(s):** `<Meta / Google Search / YouTube / ...>`
- **Landing page:** `<URL the ad points to>`

## The idea

`<one or two sentences: the angle, the emotional hook, why this audience cares>`

## Hooks (headlines to test)

1. `<hook A>`
2. `<hook B>`
3. `<hook C>`

## Primary text / script

`<the body copy or 15–30s video script beat sheet>`

## Call to action

`<one CTA, matching the landing page's primary CTA>`

## Claims (mandatory — honesty rule)

Every factual claim the ad makes, with its status. No unlabeled numbers.

| Claim in the ad | Status | Source / assumption |
|---|---|---|
| `<claim>` | measured / target / product-fact | `<query, doc, or "target">` |

## Visual direction

`<look, motion, key frames; assets to produce in Studio>`

## Do-not-say

`<claims explicitly off-limits for this concept — e.g. unverified comparisons>`
